# Simulator (Phase 1/2) — pure-numpy sim, gym env, canonical encoder.
gymnasium>=0.29
numpy>=1.24
pytest>=7.4

# RL training (Phase 3) — MaskablePPO over the sim.
# Install with the launcher on Windows:  py -m pip install torch stable-baselines3 sb3-contrib tensorboard
torch>=2.1
stable-baselines3>=2.5
sb3-contrib>=2.5
tensorboard>=2.14
